{
  "name": "@product-dev-consulting/client-communication-agent",
  "version": "1.0.0",
  "description": "Handles external communication with clients, including sending emails and Slack messages, and processing inbound client requests using Google Workspace (Gmail).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "eslint 'src/**/*.{ts,js}' --max-warnings=0",
    "test": "jest"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.4.2",
    "eslint": "^8.50.0",
    "@typescript-eslint/parser": "^5.0.0",
    "@typescript-eslint/eslint-plugin": "^5.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0"
  }
}
